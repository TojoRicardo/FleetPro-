# FleetPro API (public — safe for Vite client bundle)
VITE_API_URL=/api/v1

# Dev server (vite.config.ts only — not exposed to browser)
VITE_DEV_API_PROXY=http://localhost:9000
VITE_DEV_PORT=5173

# Realtime (optional)
VITE_ENABLE_WS=false
# VITE_WS_URL=http://localhost:6001
