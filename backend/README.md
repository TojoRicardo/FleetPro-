# FleetPro Backend

Laravel 11 API. Voir [../README.md](../README.md).

```bash
composer install && cp .env.example .env && php artisan key:generate && php artisan migrate && php artisan db:seed --class=EssentialSeeder
php artisan serve --host=127.0.0.1 --port=9000
```
