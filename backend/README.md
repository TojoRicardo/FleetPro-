# FleetPro Backend

Laravel 11 API. Voir [../README.md](../README.md).

```bash
composer install && cp .env.example .env && php artisan key:generate && php artisan migrate --seed
php artisan serve --port=9000
```
