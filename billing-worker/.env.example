# PostgreSQL (same as docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=fleetpro
DB_USERNAME=postgres
DB_PASSWORD=your_password_here

# Cron: 00:05 on the 1st of every month
INVOICE_CRON=5 0 1 * *

# Lock TTL in minutes (prevents duplicate runs)
JOB_LOCK_TTL_MINUTES=120

LOG_LEVEL=info
